[package]
name = "black_hole"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
bytemuck = { version = "1.25.0", features = ["derive"] }

[dev-dependencies]
proptest = "1.11.0"

# Fast compiles in dev
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
